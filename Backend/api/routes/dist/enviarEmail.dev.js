"use strict";

var express = require('express');

var bodyParser = require('body-parser');

var Emailrouter = express.Router();

var nodemailer = require('nodemailer');

Emailrouter.post('/enviar', function (req, res) {
  req;
});